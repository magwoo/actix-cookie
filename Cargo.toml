[package]
name = "actix-cookie"
version = "0.1.0-alpha"
description = "Cookie extractor middleware for actix-web"
license = "MIT"
keywords = ["cookie", "http", "web", "actix", "actix-web", "middleware"]
repository = "https://github.com/magwoo/actix-cookie"
edition = "2021"

[dependencies]
